# 🔁 Two Pointers / Sliding Window

투 포인터(Two Pointers)는  
**두 개의 인덱스를 움직이며 조건을 만족하는 구간이나 쌍을 찾는 기법**이다.

코딩테스트에서 투 포인터는  
👉 새로운 알고리즘이 아니라  
👉 **브루트포스를 선형으로 압축하는 사고 방식**이다.

---

## 언제 투 포인터를 떠올려야 하나?
문제에서 아래 신호가 보이면 투 포인터를 의심한다.

- 연속된 구간
- 두 값의 조합
- 합이 특정 값
- 최소 / 최대 길이
- 정렬된 배열
- 시간복잡도를 줄여야 한다

📌 핵심 신호
> **“이중 for문이 떠오르는데, 너무 느릴 것 같다”**

---

## 투 포인터의 핵심 사고
투 포인터의 본질은  
**“이미 확인한 구간은 다시 보지 않는다”**는 약속이다.

그래서 항상 이 질문을 던져야 한다.

> “포인터를 한 방향으로만 움직여도  
> 모든 경우를 커버할 수 있는가?”

YES → 투 포인터 가능  
NO → 다른 알고리즘 고민

---

## 투 포인터 사고 흐름 (공식처럼)

1. **포인터 2개 정의**
    - left, right
2. **불변 조건(invariant) 설정**
    - 구간 합
    - 거리
    - 조건 만족 여부
3. **어느 쪽 포인터를 움직일지 결정**
4. **한 번 지나간 구간은 다시 보지 않음**

📌 이 구조가 깨지면  
👉 투 포인터가 아니다.

---

## 투 포인터의 대표 유형

### 1️⃣ 양쪽에서 좁혀오는 투 포인터
- 배열이 **정렬되어 있음**
- left는 앞, right는 뒤에서 시작

📌 사용 상황
- 두 수의 합
- 가장 가까운 두 값
- 차이가 최소 / 최대

📌 핵심
> 조건에 따라 **한 쪽 포인터만 이동**

---

### 2️⃣ 같은 방향으로 이동하는 투 포인터 (Sliding Window)
- 연속 구간 유지
- right로 확장
- 조건 초과 시 left로 축소

📌 사용 상황
- 연속 부분합
- 최소 길이 / 최대 길이
- 구간 내 조건 유지

📌 핵심
> **윈도우 내부 상태를 항상 유지**

---

## Sliding Window의 핵심 감각
슬라이딩 윈도우는  
**“구간을 유지한 채 밀고 당긴다”**는 사고다.

그래서 반드시 관리해야 할 것이 있다.

- 현재 합
- 현재 개수
- 현재 조건 만족 여부

📌 이 상태 관리가 없으면  
👉 윈도우가 무너진다.

---

## 투 포인터 문제에서 반드시 점검할 것들
문제 풀기 전에 이걸 체크해라.

- 배열이 정렬되어 있는가?
- 연속 구간인가?
- 포인터가 뒤로 돌아가지 않는가?
- 불변 조건이 유지되는가?
- 전체를 한 번만 순회하는가?

---

## 투 포인터에서 자주 하는 실수
(실전에서 진짜 많이 틀린다)

- 정렬 안 하고 양쪽 포인터 사용
- 연속 구간인데 양끝 포인터 사용
- 포인터 이동 조건 반대로 설정
- 상태 값 갱신 누락
- 무한 루프

📌 투 포인터는  
👉 **조건 판단 미스 = 즉시 오답**이다.

---

## 투 포인터 vs 이진 탐색
이 차이가 보이면 실력이 확 오른다.

- 정답 하나 찾기 → 이진 탐색
- 모든 쌍 / 구간 → 투 포인터
- 범위 절반 제거 → 이진 탐색
- 선형 압축 → 투 포인터

---

## 문제 기록
(※ **직접 푼 문제만 작성**)

### [leetCode 11 - Container With Most Water](https://leetcode.com/problems/container-with-most-water/)

### <img src="https://static.solved.ac/tier_small/7.svg" width="13px" />&nbsp;[BOJ 1822 - 숫자 카드](https://www.acmicpc.net/problem/1822)
- 왜 ? : 특정 문자를 기준으로 배열을 검색하는건데 원소의 갯수가 50만개가 넘어서 2중 for문이면 O(n2) 이라 무조건 시간 초과
- 시간복잡도 : O(N log N)
- 생각 포인트 : 굳이 투포인터를 사용할 필요가 없다...그냥 HashSet 사용해도됨
---

## 한 줄 요약
> **“투 포인터는  
> 이미 본 구간을 다시 보지 않는 약속이다.”**
